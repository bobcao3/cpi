[flag]
description = "Select the current model's -fast variant"

[tool]
description = "Select or leave the current model's -fast variant"
prompt_snippet = ""

[guidelines]
bullets = []

[messages]
usage = "Usage: /fast [{{options}}]"
unavailable = "Fast model unavailable: {{model}}. Select an available model explicitly."
auth = "No usable authentication for {{model}}. Model selection was not changed."
